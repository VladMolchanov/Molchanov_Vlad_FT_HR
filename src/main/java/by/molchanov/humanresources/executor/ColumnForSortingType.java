package by.molchanov.humanresources.executor;

/**
 * Enum {@link ColumnForSortingType} contains types column, which used for sorting.
 *
 * @author MolcanovVladislav
 */
public enum ColumnForSortingType {
    SORT_BY_ORGANIZATION, SORT_BY_DATE, SORT_BY_VAC_NAME, SORT_BY_EMPTY_COLUMN, SORT_BY_USER_NAME
}
